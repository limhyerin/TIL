// javascrpit에서 말하는 object
// key - value pair

const name = "hong gil dong";
const age = 20;

const testObj = {
  name, // 위에 변수로 선언해둔 값을 그대로 가져오려면 값부분 생략가능
  age, // 생략 가능
  company: "Team Korea",
  doSomething: () => {}, // 값으로 함수도 올수있음
};
